# Neuron AI Bot

Telegram бот с интеграцией базы данных PostgreSQL и различными функциями.

## 🆕 Новое: Система контроля доступа

**Версия 1.0** включает систему ограничения доступа на 24 часа!

- ✅ Автоматическая проверка доступа для всех пользователей
- ⏰ Ограничение в 24 часа с момента `/start`
- 📊 Удобная утилита управления доступом
- 🔒 Безопасная блокировка после истечения срока

**Быстрый старт:**
- 📖 [Подробная документация](ACCESS_CONTROL_README.md)
- ⚡ [Краткая инструкция](QUICKSTART_ACCESS_CONTROL.md)
- 📝 [Сводка изменений](CHANGES_SUMMARY.md)
- 🛠 [Утилита управления](MANAGE_ACCESS_README.md)
- 💾 [SQL запросы](access_control_queries.sql)

## Структура проекта

```
demo_neuronai_bot/
├── main.py                 # Основной файл бота
├── models.py               # Модели базы данных (SQLAlchemy)
├── manage_access.py        # 🆕 Утилита управления доступом
├── requirements.txt        # Зависимости проекта
├── .env                    # Переменные окружения (не коммитить!)
├── create_users_table.sql  # SQL-запрос для создания таблицы users
├── access_control_queries.sql  # 🆕 SQL запросы для управления
├── src/
│   └── Neira.mp4          # Видео для приветствия
└── handlers/              # Обработчики кнопок
    ├── __init__.py
    ├── hr.py              # 🤝 HR и Найм
    ├── labor_safety.py    # 👷‍♂️ Охрана труда
    ├── it_helpdesk.py     # 🛠 IT HelpDesk
    ├── knowledge_base.py  # 🧠 База Знаний
    └── ai_manager.py      # 💰 AI-Менеджер
```

## Настройка

1. Создайте файл `.env` со следующими переменными:
```
BOT_TOKEN=your_telegram_bot_token
DATABASE_URL=postgresql+psycopg2://username:password@host:port/database
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите бота:
```bash
python main.py
```

4. (Опционально) Запустите утилиту управления доступом:
```bash
python manage_access.py
```

## Функциональность

### База данных
При запуске бота автоматически создается таблица `users` со следующей структурой:
- `id` - внутренний первичный ключ
- `telegram_id` - уникальный ID пользователя Telegram
- `started_at` - дата и время первого запуска бота пользователем (с часовым поясом)

### 🆕 Система контроля доступа
- Каждый пользователь получает **24 часа** доступа с момента `/start`
- Автоматическая проверка при каждом взаимодействии
- После истечения доступа:
  - Показывается сообщение с информацией
  - Предлагается связаться с менеджером
  - Блокируется использование бота
- Кнопка "🔄 Проверить доступ" показывает оставшееся время

### Обработчики
При нажатии на кнопки пользователь получает сообщение-заглушку о том, что функционал в разработке.

Доступные кнопки:
- 🤝 HR и Найм
- 👷‍♂️ Охрана труда
- 🛠 IT HelpDesk
- 🧠 База Знаний
- 💰 AI-Менеджер

## SQL-запрос для ручного создания таблицы

Если нужно создать таблицу вручную в PostgreSQL, используйте файл `create_users_table.sql`:
```bash
psql -h host -U username -d database -f create_users_table.sql
```

## Коммит и пуш в Git

```bash
git add .
git commit -m "Add video greeting and handlers for bot buttons"
git push
```

