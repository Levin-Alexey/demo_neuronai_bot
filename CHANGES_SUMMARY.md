# 📋 Сводка изменений: Система контроля доступа

## 🎯 Цель
Ограничить использование бота для пользователей временем в 24 часа с момента первого запуска `/start`. После истечения доступа показывать сообщение о необходимости связи с менеджером.

## ✅ Реализованные изменения

### 1️⃣ **models.py**
Добавлена функция проверки доступа:
```python
def check_user_access(session, telegram_id: int) -> tuple[bool, datetime | None]
```
- Проверяет, прошло ли 24 часа с момента `started_at`
- Возвращает `(имеет_доступ, дата_окончания_доступа)`
- Использует timezone-aware datetime (UTC)

### 2️⃣ **main.py**
Добавлен middleware для автоматической проверки:
```python
class AccessCheckMiddleware(BaseMiddleware)
```
**Что делает:**
- Проверяет доступ перед каждым действием пользователя
- Пропускает команду `/start` (для новых пользователей)
- При истекшем доступе:
  - Показывает сообщение с информацией
  - Предлагает кнопки "Связаться с менеджером" и "Проверить доступ"
  - **Блокирует дальнейшую работу с ботом**

**Новые обработчики:**
1. `check_access_status()` - кнопка "🔄 Проверить доступ"
   - Показывает оставшееся время для активного доступа
   - Показывает сообщение об истечении для неактивного

2. `contact_manager_expired()` - кнопка "👤 Связаться с менеджером"
   - Открывает форму для отправки сообщения менеджеру
   - Использует существующий функционал из `ai_manager.py`

## 📁 Новые файлы документации

1. **ACCESS_CONTROL_README.md** - Полная документация системы
   - Описание работы
   - Технические детали
   - Настройки и расширения
   - Тестирование

2. **QUICKSTART_ACCESS_CONTROL.md** - Быстрый старт
   - Краткая инструкция
   - Основные команды
   - Решение проблем

3. **access_control_queries.sql** - SQL запросы
   - Просмотр пользователей
   - Продление доступа
   - Статистика и аналитика
   - Представления (views)

## 🔄 Сценарий использования

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Новый пользователь                                       │
└─────────────────────────────────────────────────────────────┘
/start → Регистрация в БД → started_at = NOW() → 24 часа

┌─────────────────────────────────────────────────────────────┐
│ 2. В течение 24 часов                                       │
└─────────────────────────────────────────────────────────────┘
Любая кнопка → Middleware проверяет → Доступ есть → Работает

🔄 Проверить доступ → Показывает: "Осталось X ч. Y мин."

┌─────────────────────────────────────────────────────────────┐
│ 3. После 24 часов                                           │
└─────────────────────────────────────────────────────────────┘
Любая кнопка → Middleware проверяет → Доступ истек → Блокировка
                                                          ↓
                    ⏰ Доступ к боту истек
                    
                    Ваш пробный период (24 часа) закончился.
                    
                    [👤 Связаться с менеджером]
                    [🔄 Проверить доступ]

👤 Связаться с менеджером → Форма обратной связи
🔄 Проверить доступ → "Доступ истек. Свяжитесь с менеджером."

┌─────────────────────────────────────────────────────────────┐
│ 4. Продление доступа (вручную в БД)                        │
└─────────────────────────────────────────────────────────────┘
UPDATE users SET started_at = NOW() WHERE telegram_id = ...
                            ↓
           Пользователь получает новые 24 часа
```

## ⚙️ Конфигурация

### Изменить период доступа
`models.py`, строка ~225:
```python
access_until = user.started_at + timedelta(hours=24)  # ← Здесь
```

### Изменить часовой пояс
`main.py`, строки ~64, ~175:
```python
local_time = access_until + timedelta(hours=3)  # ← MSK (UTC+3)
```

Для других часовых поясов:
- **UTC** (Лондон): `0`
- **EST** (Нью-Йорк): `-5`
- **PST** (Лос-Анджелес): `-8`
- **IST** (Индия): `5.5`
- **JST** (Токио): `9`

## 🧪 Тестирование

### Симуляция истечения доступа
```sql
UPDATE users 
SET started_at = NOW() - INTERVAL '25 hours' 
WHERE telegram_id = YOUR_TELEGRAM_ID;
```

### Возврат нормального доступа
```sql
UPDATE users 
SET started_at = NOW() 
WHERE telegram_id = YOUR_TELEGRAM_ID;
```

### Проверка в боте
1. Запустите бота
2. Нажмите любую кнопку → Должно показать сообщение об истечении
3. Нажмите "🔄 Проверить доступ" → Подтверждение истечения
4. Нажмите "👤 Связаться с менеджером" → Форма обратной связи

## 📊 Мониторинг

### SQL - Активные пользователи
```sql
SELECT COUNT(*) FROM users 
WHERE started_at + INTERVAL '24 hours' > NOW();
```

### SQL - Истекшие пользователи
```sql
SELECT COUNT(*) FROM users 
WHERE started_at + INTERVAL '24 hours' < NOW();
```

### Представления (для удобства)
```sql
-- Создать
CREATE VIEW active_users AS ...
CREATE VIEW expired_users AS ...

-- Использовать
SELECT * FROM active_users;
SELECT * FROM expired_users;
```

## 🔒 Безопасность

✅ **Реализовано:**
- Проверка на `None` для `event_update` и `from_user`
- Обработка ошибок БД (failsafe - при ошибке доступ не блокируется)
- Timezone-aware datetime (все в UTC)
- Команда `/start` всегда работает (для новых пользователей)

✅ **Защита:**
- SQLAlchemy ORM (защита от SQL-injection)
- Идемпотентность `ensure_user_started()` (повторный `/start` не меняет `started_at`)

## 🚀 Следующие шаги (опционально)

1. **Подписки**
   - Добавить поле `subscription_until` в модель `User`
   - Разные типы подписок (trial, basic, premium)

2. **Уведомления**
   - За 3 часа до истечения
   - Напоминания о продлении

3. **Аналитика**
   - Дашборд с метриками
   - Отчеты по использованию

4. **Автоматизация продления**
   - Интеграция с платежными системами
   - Промокоды и рефералки

## 📞 Поддержка

При проблемах проверьте:
1. **БД подключение** - `.env` файл с `DATABASE_URL`
2. **Middleware регистрация** - `dp.message.middleware(AccessCheckMiddleware())`
3. **Логи** - ошибки выводятся в консоль
4. **Часовой пояс** - UTC в БД, локальный в отображении

---

**Дата реализации:** 13 января 2026  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
