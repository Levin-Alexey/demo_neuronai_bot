# 🔧 Отладка: Система не блокирует доступ

## ❌ Проблема
Бот пускает пользователя даже когда `started_at` установлен на -2 дня.

## ✅ Решение

### Шаг 1: Проверьте логи бота

При запуске бота смотрите консоль на сообщения:

```
[ACCESS CHECK] User 123456789: has_access=False, access_until=2026-01-11 15:30:00+00:00
```

Если вы видите `has_access=False` - middleware **работает**!

**Если нет ошибок в логе** → middleware может быть не зарегистрирована

### Шаг 2: Используйте скрипт отладки

```bash
python debug_access.py
# Введите ваш Telegram ID
```

Вы увидите:
- Найден ли пользователь в БД
- Какое значение `started_at`
- Что вычислено `access_until`
- Правильно ли сравнивается с `NOW()`

### Шаг 3: Проверьте консоль бота

Запустите бота и смотрите в консоль:

```bash
python main.py
```

Ищите строку:
```
✅ Бот запущен. Middleware для проверки доступа активен.
```

Если её нет → проблема в main.py

### Шаг 4: Проверьте в БД

```sql
SELECT telegram_id, started_at, 
       started_at + INTERVAL '24 hours' as access_until,
       NOW() as current_time,
       (started_at + INTERVAL '24 hours') - NOW() as time_left
FROM users
WHERE telegram_id = YOUR_TELEGRAM_ID;
```

Должно быть:
- `started_at` = время в прошлом
- `access_until` = started_at + 24ч
- `time_left` = отрицательное число (если истек)

## 🐛 Возможные причины и решения

### Причина 1: Middleware не зарегистрирована

**Признак:** Нет сообщения "Доступ истек" при нажатии на кнопку

**Решение:** Проверьте main.py

```python
# main.py, в функции main():
async def main():
    init_db()
    
    # ✅ ДОЛЖНО БЫТЬ ПЕРЕД обработчиками!
    dp.message.middleware(AccessCheckMiddleware())
    
    # Потом регистрируются обработчики
    hr.register_handlers(dp)
    ...
```

### Причина 2: Неправильный formат `started_at` в БД

**Признак:** `debug_access.py` показывает странное значение `started_at`

**Решение:** Проверьте что БД хранит datetime с timezone

```sql
-- Проверить тип колонки
SELECT column_name, data_type 
FROM information_schema.columns
WHERE table_name = 'users' AND column_name = 'started_at';

-- Должно быть: timestamp with time zone
```

### Причина 3: Проблема с timezone

**Признак:** `debug_access.py` показывает `tzinfo=None`

**Решение:** Убедитесь что используется UTC

```python
# models.py
from datetime import datetime, timezone

# ✅ ПРАВИЛЬНО
now = datetime.now(timezone.utc)

# ❌ НЕПРАВИЛЬНО
now = datetime.now()  # без timezone!
```

### Причина 4: Middleware есть, но не вызывается

**Признак:** Логов с `[ACCESS CHECK]` нет в консоли

**Решение:** Добавьте логирование в middleware

```python
async def __call__(self, handler, event, data):
    print(f"🔍 Middleware вызвана!")  # Добавьте эту строку
    # ... остальной код
```

## 📋 Чек-лист

- [ ] Прочитал логи при запуске бота
- [ ] Видно ли сообщение "✅ Бот запущен. Middleware активна"?
- [ ] Запустил `debug_access.py` и проверил `started_at`
- [ ] Проверил в БД что значение в прошлом
- [ ] Проверил main.py что middleware зарегистрирована ПЕРЕД обработчиками
- [ ] Нажал на кнопку в боте и посмотрел консоль на `[ACCESS CHECK]`

## 🚀 Быстрая проверка

```bash
# 1. Запустите отладку
python debug_access.py
# Введите ваш telegram_id

# 2. Посмотрите результат
# Если видите "ДОСТУП ИСТЕК" → система работает, нужно проверить middleware

# 3. Запустите бота
python main.py
# В консоли должны появиться логи [ACCESS CHECK]

# 4. Нажмите кнопку в боте
# Должно либо заблокировать либо выполнить действие
```

## 💡 Дополнительно

### Если нужно полностью перезагрузить систему:

1. Удалить запись пользователя из БД:
```sql
DELETE FROM users WHERE telegram_id = YOUR_ID;
```

2. Отправить `/start` в боте заново

3. Это создаст новую запись с `started_at = NOW()`

4. После этого проверить доступ через 24+ часа (или установить в БД вручную)

---

**После исправления перезагрузите бота:**
```bash
# Завершите старый процесс (Ctrl+C)
# Запустите заново
python main.py
```
