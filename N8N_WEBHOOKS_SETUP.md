# Настройка n8n Webhooks для раздела "Охрана труда"

## Общая информация

Все webhook'и должны принимать POST-запросы с JSON payload и возвращать JSON-ответ.

---

## 1. Photo Control Webhook

**URL:** `https://levinbiz.app.n8n.cloud/webhook/photo-control`

### Входные данные (POST JSON):
```json
{
  "telegram_id": 123456789,
  "username": "user123",
  "file_url": "https://api.telegram.org/file/bot<token>/photos/file.jpg",
  "file_id": "AgACAgIAAxkB...",
  "caption": "Фото моего СИЗ"
}
```

### Ожидаемый ответ:
```json
{
  "analysis": "✅ Допуск разрешен.\n\nПроверка СИЗ:\n✓ Каска присутствует\n✓ Защитные очки\n✓ Спецодежда\n\nМожете приступать к работе.",
  "status": "approved"
}
```

### Логика обработки:
1. Получить изображение по `file_url`
2. Отправить в Vision AI для анализа СИЗ
3. Проверить наличие:
   - Каски
   - Защитных очков
   - Спецодежды
   - Других необходимых СИЗ
4. Сформировать текстовый результат
5. Вернуть ответ с рекомендациями

---

## 2. Work Permit Webhook

**URL:** `https://levinbiz.app.n8n.cloud/webhook/work-permit`

### Входные данные (POST JSON):
```json
{
  "telegram_id": 123456789,
  "username": "user123",
  "full_name": "Иван Иванов",
  "work_type": "electrical",
  "work_type_text": "⚡ Электротехнические работы",
  "location": "Цех №2, участок сборки",
  "duration": "2 часа",
  "description": "Замена электропроводки. Требуется отключение питания и СИЗ"
}
```

### Ожидаемый ответ:
```json
{
  "permit_number": "НД-2024-001234",
  "message": "Наряд-допуск успешно оформлен и отправлен службе охраны труда на утверждение."
}
```

### Логика обработки:
1. Генерировать уникальный номер наряда-допуска
2. Сохранить данные в базе/таблице
3. Отправить уведомление ответственному лицу (email/Telegram)
4. Сформировать PDF-документ наряда (опционально)
5. Вернуть номер наряда пользователю

---

## 3. Report Violation Webhook

**URL:** `https://levinbiz.app.n8n.cloud/webhook/report-violation`

### Входные данные (POST JSON):
```json
{
  "telegram_id": 123456789,
  "username": "user123",
  "full_name": "Анонимный сотрудник",
  "violation_type": "fire_hazard",
  "violation_type_text": "🔥 Пожарная опасность",
  "location": "Складское помещение А",
  "description": "Заблокирован пожарный выход коробками с товаром",
  "photo_url": "https://api.telegram.org/file/bot<token>/photos/file.jpg",
  "photo_file_id": "AgACAgIAAxkB..."
}
```

### Ожидаемый ответ:
```json
{
  "report_number": "ОТ-2024-005678",
  "message": "Ваше сообщение принято в работу. Служба охраны труда выезжает на место в течение 30 минут."
}
```

### Логика обработки:
1. Генерировать уникальный номер обращения
2. Сохранить данные о нарушении
3. Оценить критичность нарушения (автоматически или через AI)
4. Отправить экстренное уведомление при критических нарушениях
5. Создать задачу для службы ОТ
6. Если есть фото - сохранить и приложить к обращению
7. Вернуть номер обращения

---

## 4. Bot Instructor Webhook

**URL:** `https://levinbiz.app.n8n.cloud/webhook/bot-instructor`

### Входные данные для запроса (POST JSON):
```json
{
  "telegram_id": 123456789,
  "username": "user123",
  "full_name": "Иван Иванов",
  "topic": "fire_safety",
  "topic_text": "🔥 Пожарная безопасность",
  "query": "Что делать при пожаре в офисе?",
  "action": "query"
}
```

### Входные данные для завершения сессии:
```json
{
  "telegram_id": 123456789,
  "action": "end_session"
}
```

### Ожидаемый ответ:
```json
{
  "answer": "При обнаружении пожара в офисе необходимо:\n\n1. 🚨 Немедленно сообщить в службу 112\n2. 🔔 Активировать пожарную сигнализацию\n3. 🚪 Эвакуировать всех сотрудников\n4. 🔥 При возможности - использовать огнетушитель\n5. ❌ Не пользоваться лифтом\n6. 📍 Собраться в точке сбора\n\nПомните: безопасность людей важнее имущества!"
}
```

### Логика обработки:
1. Определить контекст вопроса (тему ОТ)
2. Отправить запрос в AI (GPT/Claude) с промптом инструктора по ОТ
3. Получить развернутый ответ
4. Форматировать ответ с эмодзи и структурой
5. Логировать вопросы для аналитики
6. При завершении сессии - сохранить статистику обучения

### Промпт для AI:
```
Ты - опытный инструктор по охране труда и промышленной безопасности.
Твоя задача - обучать сотрудников правилам безопасности, отвечать на вопросы 
и давать практические рекомендации.

Отвечай:
- Четко и структурированно
- С примерами из практики
- Используй нумерованные списки
- Добавляй важные предупреждения
- Ссылайся на нормативы (если применимо)

Тема: {topic_text}
Вопрос: {query}

Дай развернутый практический ответ.
```

---

## Общие рекомендации по настройке n8n

### 1. Структура Workflow

Каждый webhook должен иметь:
```
Webhook Node
    ↓
Function Node (парсинг входных данных)
    ↓
HTTP Request / AI Node (обработка)
    ↓
Function Node (форматирование ответа)
    ↓
Respond to Webhook Node
```

### 2. Обработка ошибок

Добавить узел Error Trigger для каждого workflow:
- Логирование ошибки
- Отправка уведомления админу
- Возврат корректного JSON-ответа

### 3. Таймауты

Установить таймауты:
- Для Vision AI: 60 секунд
- Для обычных запросов: 30 секунд

### 4. Логирование

Рекомендуется логировать:
- Все входящие запросы
- Ответы от внешних сервисов
- Ошибки
- Время обработки

### 5. База данных

Для сохранения данных можно использовать:
- Google Sheets (простой вариант)
- PostgreSQL (для production)
- Airtable (удобный интерфейс)

---

## Тестирование webhook'ов

### Используя curl:

```bash
# Photo Control
curl -X POST https://levinbiz.app.n8n.cloud/webhook/photo-control \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": 123456789,
    "username": "test",
    "file_url": "https://example.com/test.jpg",
    "file_id": "test123",
    "caption": "Test photo"
  }'

# Work Permit
curl -X POST https://levinbiz.app.n8n.cloud/webhook/work-permit \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": 123456789,
    "username": "test",
    "full_name": "Test User",
    "work_type": "repair",
    "work_type_text": "Ремонтные работы",
    "location": "Тестовый цех",
    "duration": "1 час",
    "description": "Тестовое описание"
  }'

# Report Violation
curl -X POST https://levinbiz.app.n8n.cloud/webhook/report-violation \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": 123456789,
    "username": "test",
    "full_name": "Test User",
    "violation_type": "safety_violation",
    "violation_type_text": "Нарушение ТБ",
    "location": "Тестовое место",
    "description": "Тестовое описание",
    "photo_url": "",
    "photo_file_id": ""
  }'

# Bot Instructor
curl -X POST https://levinbiz.app.n8n.cloud/webhook/bot-instructor \
  -H "Content-Type: application/json" \
  -d '{
    "telegram_id": 123456789,
    "username": "test",
    "full_name": "Test User",
    "topic": "fire_safety",
    "topic_text": "Пожарная безопасность",
    "query": "Что делать при пожаре?",
    "action": "query"
  }'
```

---

## Мониторинг

Рекомендуется настроить:
1. Уведомления при ошибках в workflow
2. Дашборд с количеством запросов
3. Отслеживание времени ответа
4. Алерты при превышении таймаутов

---

## Безопасность

1. **Валидация входных данных** - проверять наличие обязательных полей
2. **Rate limiting** - ограничить количество запросов от одного пользователя
3. **Хранение токенов** - использовать environment variables в n8n
4. **Логирование без чувствительных данных** - не логировать токены и URL с токенами

---

Дата создания: 2024
Версия: 1.0

